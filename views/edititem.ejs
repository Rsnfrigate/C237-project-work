<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">

</head>

<body class="container py-4">

    <h1>Edit Laptop Page</h1>

    <div>

        <form action="/editProduct/<%= laptops.id %>" method="POST">

            <div class="mb-3">

                <label for="model">Laptop Model</label>

                <input type="text" class="form-control" id="model" name ="model"
                    placeholder="Enter Laptop Model" value="<%= laptops.model %>">



            </div>


            <div class="mb-3">

                <label for="status">Status of Laptop</label>

                <select name="status" class="form-select me-2">

                    <option value="available" <%= laptops.status === 'available' ? 'selected' : ' ' %>>available</option>
                    <option value="in use" <%= laptops.status === 'in use' ? 'selected' : ' ' %>>in use</option>
                    <option value="maintenance" <%= laptops.status === 'maintenance' ? 'selected' : ' ' %>>maintenance</option>

                </select>

            </div>
                
            <button type="submit" class="btn btn-primary">Submit</button>

        </form>


    </div>

</body>

</html>